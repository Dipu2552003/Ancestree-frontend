import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      colors: {
        saffron: '#EA580C',
        marigold: '#D97706',
        terracotta: '#C2410C',
        cream: '#FFF7ED',
        goldLight: '#FDE8CC',
      },
    },
  },
}

export default config
